A small Swift program to register `ssh:` URL handler on Macs that forwards to
Ghostty to open a new tab and run the ssh command. For example, if your webapp has `ssh://mymachine` links, you can now click it and it should forward to Ghostty.

Run `./install.sh` to build and install it to `~/Applications`
